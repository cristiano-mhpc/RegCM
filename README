This is the profiling branch of the RegCM on the ORFEO HPC cluster. 
=========================================

This feauture branch of RegCM includes offloading of the `getcape` subroutine using a combination of `OpenACC` and `do concurrent`.  
